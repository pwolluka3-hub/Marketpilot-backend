import GlassCard from '../components/ui/GlassCard';

/**
 * Render the central agent dashboard UI.
 *
 * Renders a GlassCard containing the dashboard content (queue and quick-generate entry).
 * @returns {JSX.Element} The dashboard React element.
 */
export default function Dashboard() {
  return <GlassCard>Central agent dashboard with queue + quick generate.</GlassCard>;
}