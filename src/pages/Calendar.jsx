/**
 * Render a placeholder calendar card labeled "Calendar + drag-to-reschedule placeholder."
 *
 * @returns {JSX.Element} A div with `className="glass-card"` containing the placeholder text.
 */
export default function Calendar() {
  return <div className="glass-card">Calendar + drag-to-reschedule placeholder.</div>;
}