/**
 * Renders a placeholder calendar view used for the drag-to-reschedule UI.
 *
 * @returns {JSX.Element} A div element with class "glass-card" containing the text "Calendar + drag-to-reschedule placeholder."
 */
export default function Calendar() {
  return <div className="glass-card">Calendar + drag-to-reschedule placeholder.</div>;
}