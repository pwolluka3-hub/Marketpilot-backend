/**
 * Renders the AIChat page container with the visible label for the long-memory chat workspace.
 * @returns {JSX.Element} A <div> element with class "glass-card" containing the text "Long-memory AI chat workspace."
 */
export default function AIChat() {
  return <div className="glass-card">Long-memory AI chat workspace.</div>;
}