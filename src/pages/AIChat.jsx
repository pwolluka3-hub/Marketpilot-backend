/**
 * Renders the AI chat workspace container.
 *
 * @returns {JSX.Element} A <div> element with className "glass-card" that contains the text "Long-memory AI chat workspace."
 */
export default function AIChat() {
  return <div className="glass-card">Long-memory AI chat workspace.</div>;
}
